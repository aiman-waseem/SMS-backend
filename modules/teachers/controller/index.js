export const createTeacherController = (req, reply) => {
    // Logic to create a new teacher
    // Example: Extract data from req.body, validate it, and save to the database
    reply.send({ message: "Teacher created successfully" });
}